[phases.setup]
nixPkgs = ["nodejs_20", "python311", "python311Packages.pip"]

[phases.install]
cmds = ["npm ci", "python -m pip install --break-system-packages -r requirements.txt"]

[phases.build]
cmds = ["npm run build"]
